name: c3
image: okteto/python:3
command: bash
volumes:
- /root/.cache/pip
sync:
- .:/usr/src/app
# forward:
#  - 8080:8080
# reverse:
#  - 9000:9000
resources:
  requests:
    cpu: "128"
    memory: "512Gi"
  limits:
    cpu: "256"
    memory: "1024Gi"
autocreate: true
